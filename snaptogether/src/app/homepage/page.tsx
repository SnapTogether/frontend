import Button from "@/components/Button/Button";

export default function Homepage() {
    

    return (
        <div>
            <Button variant="primary">
                Primary Button
            </Button>
        </div>
    );
}
 